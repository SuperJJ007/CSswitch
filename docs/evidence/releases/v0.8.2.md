# CSSwitch v0.8.2 发布证据

复核时间：2026-07-23（Asia/Shanghai）。本文件分别记录 release source、自动门禁、最终 DMG、安装 UI 与公开分发；自动、mock/loopback 与本机 UI 证据不外推为所有真实 provider、账号或 SSH server 均已通过。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| release source / 发布时 `origin/main` | `0e740814c5cb30d7623757231ced882767f28a53` |
| annotated tag object | `3e54ed969163392c2e718d1af3c0035f16c757b6`；unsigned |
| peeled `v0.8.2^{}` | `0e740814c5cb30d7623757231ced882767f28a53` |
| 版本 / 架构 | `v0.8.2` / macOS Apple Silicon |
| GitHub Release | `CSSwitch v0.8.2`；已公开，非 draft、非 prerelease |
| 发布时间 | `2026-07-23T07:55:29Z` |
| 页面 | `https://github.com/SuperJJ007/CSSwitch/releases/tag/v0.8.2` |

发布 tag 固定在 release source；本证据文件在公开附件回读后补写，因此后续 `main` 可以领先 tag，release identity 仍以 peeled tag 为准。

## 自动门禁与独立复审

在从 v0.8.1 建立的独立干净 worktree 上运行 `bash test/run_all.sh --require-release-ready`，命令退出 0，offline、loopback、scripts、rust、frontend 五层通过并汇总为 `release-ready green: YES`。

- loopback 102 passed；脚本与 real-machine guard passed；
- Desktop 378 passed、7 个明确 installed/real-Science 测试 ignored；
- Gateway 272 lib + 1 CLI passed；
- `cargo fmt`、`cargo clippy -- -D warnings`、前端门禁和 `git diff --check` 通过；
- `npm ci` 报告 0 vulnerabilities，`npm run tauri build` 从同一 release source 产出 arm64 app 与 DMG。

Codex 动态目录冷启动、`python` 工具名转换和 coverage fixture 超时分别经过独立只读复审。动态目录根因确认为首次 `/v1/models` 错用了 400 ms 本地目录预算；修复后只发送一次请求并使用既有 130,750 ms 有界预算，静态 provider 仍保持 400 ms。复审没有发现 P0/P1 发布阻断。

Tauri 首轮 app 外层 resource seal 不完整，`codesign --verify --deep --strict` 正确阻断了该候选。随后先对 Gateway、再对外层 app 执行本地 ad-hoc seal，并从只含正式 app 与 Applications 链接的一次性 staging 目录生成最终 DMG；首轮候选没有上传。

## 最终附件身份

| 项目 | 值 |
|---|---|
| 文件 | `CSSwitch_0.8.2_aarch64.dmg` |
| 大小 | 12,697,639 bytes |
| SHA-256 | `35c78c38773df119346ec9e1a629cce097d77cd45844bb32cd469a304e53911a` |
| Desktop SHA-256 | `fbc588db9bd3c1b9dd2ad7c0ea42b87bf1d518963abd0a6512a189fcab312424` |
| Gateway SHA-256 | `de40607f760a1d3841377044a192487c73521af3edbcfb74b0c0de483a4dd1fa` |
| GitHub asset | `state=uploaded`；`application/x-apple-diskimage` |
| GitHub size / digest | 12,697,639 bytes / `sha256:35c78c38773df119346ec9e1a629cce097d77cd45844bb32cd469a304e53911a` |
| 重新下载复核 | SHA-256 与本地最终 DMG 一致 |

本地最终 DMG 和公开重新下载附件均通过 `hdiutil verify`，并分别只读挂载。根目录精确只有一个 `CSSwitch.app` 和指向 `/Applications` 的 Applications 链接；app 版本为 0.8.2，Desktop 与 Gateway 均为 Mach-O arm64，挂载后二进制与 clean build 一致。

`codesign --verify --deep --strict --verbose=4` 对 clean build、安装 app 和公开附件挂载 app 均通过；外层身份为 ad-hoc、无 Team ID。这只证明 bundle seal 完整，不表示 Developer ID、notarization 或 Gatekeeper acceptance。

## 安装与 UI 验收

经用户授权，原 `/Applications/CSSwitch.app` 先移入废纸篓作可恢复备份，最终 clean-build app 覆盖安装到 `/Applications/CSSwitch.app`。`/Applications` 与用户 Applications 目录只检测到这一个 CSSwitch app；安装后二进制 hash 与最终 app 一致，真实配置与密钥未删除或输出。

在最终安装版上连续执行三轮“官方 Claude → 第三方模型 → 一键开始”：

- 每轮第一次点击即完成，不需要再次点击；
- 没有出现 `gateway_start` 或 `catalog_verify` 错误；
- 冷启动期间 UI 保持“仍在准备”，最终代理与 Science 均显示“运行正常”；
- Desktop/Gateway 从 `/Applications/CSSwitch.app` 运行，Science 从 CSSwitch 管理的内容寻址 runtime snapshot 运行；
- profile 卡片仍显示 OpenCode Go `kimi-k3`。

本轮没有发送真实 provider 推理请求，没有读取或输出完整 API key，也没有用虚构 SSH server 声称建立真实远端连通性。

## 本次修复边界

- Claude Science 隔离 HOME 的 SSH 前置检查现在消费具体授权 Host 块，不再依赖 Science 会跳过的跨 HOME `Include`。
- OpenCode Go `kimi-k3` 在 profile、selector、`/v1/models` 与上游路由间保持一致。
- 旧版到新版 coverage 安装流程、更新后的 Science runtime snapshot、Codex `python` 工具名兼容和首次动态目录冷启动均有回归覆盖。
- 第三方 Science 仍以 `--no-auto-update` 运行；应先让官方 Science 更新，再停止并重新启动 CSSwitch 管理链以采用经验证的新 runtime。

## 未建立或不在本次声明中的层

- 最终公开 artifact 上的真实 OpenCode Go、Kimi、DeepSeek、Grok、Gemini 或 Codex 推理；
- 无真实 SSH server 环境下的远端命令执行；
- Developer ID、Apple notarization、stapled ticket 与 Gatekeeper acceptance；
- macOS Intel、Windows 或 WSL 支持；Linux 继续使用已发布的 v0.8.1 Linux x64 prerelease。
