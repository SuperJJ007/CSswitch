# CSSwitch v0.8.1 发布证据

复核时间：2026-07-20（Asia/Shanghai）。本文件分别记录 release source、自动门禁、最终 DMG、安装 UI 与公开分发；自动和 mock/loopback 证据不外推为所有真实 provider、账号或 Science 环境均已通过。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| release source / 发布时 `origin/main` | `c93c7e64d75703d38f08c385ed94460b5057831b` |
| annotated tag object | `700d955f97584b19ac9f8734c580735db78b503e`；unsigned |
| peeled `v0.8.1^{}` | `c93c7e64d75703d38f08c385ed94460b5057831b` |
| 版本 / 架构 | `v0.8.1` / macOS Apple Silicon |
| GitHub Release | `CSSwitch v0.8.1`；已公开，非 draft、非 prerelease |
| 发布时间 | `2026-07-20T11:53:12Z` |
| 页面 | `https://github.com/SuperJJ007/CSSwitch/releases/tag/v0.8.1` |

发布 commit 建立在当时最新 `origin/main` `94f7ee88b30c9ca9768cd4deb679df26206e79a1` 之上。0.8.1 commit 未修改 `README.md`、`README.en.md` 或 `docs/README.md`；发布证据在公开回读后补写，因此后续 `main` 可领先 tag，release identity 仍以 peeled tag 为准。

## 自动门禁与 clean build

在 release source 的独立干净 worktree 上运行 `bash test/run_all.sh --require-release-ready`，命令退出 0，offline、loopback、scripts、rust、frontend 五层通过并汇总为 `release-ready green: YES`。

- offline 22 passed；loopback 99 passed；脚本与 real-machine guard passed；
- Desktop 361 passed、5 个明确 installed/real-Science 测试 ignored；
- Gateway 271 lib + 1 CLI passed；
- 前端结构化认证 3、Skill 状态 9、runtime 状态 5、模型目录/UI 15，均 passed；
- `npm ci` 报告 0 vulnerabilities，`npm run tauri build` 从同一 commit 产出 arm64 app 与 DMG。

Tauri 首轮 app 外层 resource seal 不完整，`codesign --verify --deep --strict` 正确阻断了该候选。随后先对 Gateway、再对外层 app 执行本地 ad-hoc seal，并从一次性空 staging 目录生成最终 DMG；首轮候选没有上传。

## 最终附件身份

| 项目 | 值 |
|---|---|
| 文件 | `CSSwitch_0.8.1_aarch64.dmg` |
| 大小 | 11,392,224 bytes |
| SHA-256 | `923f2977ec4e4dd83787a6b928867bf0141571c5814ce8cdc84a84b64d750bef` |
| Desktop SHA-256 | `a70f0fdd67952d434f849f92fd9d761c4a92c299cfae5b95668066d0e1493802` |
| Gateway SHA-256 | `52da0fe62cd9ced0497aa7e2d57dd572b33c33f77045aad81bd05008ff12546d` |
| GitHub asset | `state=uploaded`；`application/x-apple-diskimage` |
| GitHub size / digest | 11,392,224 bytes / `sha256:923f2977ec4e4dd83787a6b928867bf0141571c5814ce8cdc84a84b64d750bef` |
| 重新下载复核 | 与本地最终 DMG `cmp` 逐字节一致 |

本地最终 DMG 和公开重新下载附件均通过 `hdiutil verify`，并分别只读挂载。根目录精确只有一个 `CSSwitch.app` 和指向 `/Applications` 的 Applications 链接；app 数量为 1，不含 Test 或 Acceptance app。app 版本为 0.8.1、bundle ID 为 `com.csswitch.menubar`，Desktop 与 Gateway 均为 Mach-O arm64，挂载后二进制与 clean build 逐字节一致。包内包含预期 scripts，不含旧 `Contents/Resources/proxy`。

`codesign --verify --deep --strict --verbose=4` 对 clean build、安装 app 和公开附件挂载 app 均通过；外层身份为 `Signature=adhoc`、`TeamIdentifier=not set`。这只证明 bundle seal 完整，不表示 Developer ID、notarization 或 Gatekeeper acceptance。

## 安装与 UI 验收

经用户授权，最终 clean-build app 已覆盖安装到 `/Applications/CSSwitch.app`，原 app 先移动到可恢复的临时备份。安装后二进制 hash 与最终 app 一致。

使用安装版 UI 只读检查：

- 页面显示 `v0.8.1`；
- DeepSeek 卡片显示“默认/均衡/快速：DeepSeek V4 Flash；高质量/Fable：DeepSeek V4 Pro”；
- 新建 Kimi 配置的默认模型为 `kimi-k3`；
- OpenCode Go 双协议、Grok 与 Gemini 出现在 provider 列表；
- 既有 OpenCode Go 配置保留原模型，没有被 preset 更新静默迁移。

本轮 UI 验收没有发送 provider 推理请求，没有消耗 Grok/Gemini API，也没有读取或输出完整 API key。历史恢复的 marker、多候选、目录身份重验和 v0.8.0 兼容路径由精确 Rust 测试覆盖；最终安装版没有构造真实多组织 Sign out 数据集。

## Issue 处置

- `#63` 多历史组织 UI 恢复：回复后关闭，关闭时间 `2026-07-20T11:57:36Z`；
- `#8` DeepSeek DSML 泄漏：回复后关闭，关闭时间 `2026-07-20T11:57:37Z`；
- `#56`、`#12`、`#55`、`#19`、`#11`：均已回复并保持打开，分别等待 provider 分类、运行时守护、可复现信息、用户机器 runtime 复测和通用 MCP upstream proxy 实现。

## 未建立或不在本次声明中的层

- 最终公开 artifact 上的真实 Grok、Gemini、OpenCode Go、Kimi、DeepSeek 或 Codex 账号推理；
- 真实多组织 Science Sign out 后的最终安装版端到端恢复；
- Claude Science 自身 SSH 前置解析器接受 sandbox `Include` bridge；自动测试只证明系统 OpenSSH 合同；
- Developer ID、Apple notarization、stapled ticket 与 Gatekeeper acceptance；
- macOS Intel、Windows、Linux 或 WSL 支持。
