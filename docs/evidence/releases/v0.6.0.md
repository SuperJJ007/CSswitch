# CSSwitch v0.6.0 发布证据

复核时间：2026-07-16（Asia/Shanghai）。本文件区分源码闸门、发布者人工验收、最终 artifact 与公开分发；发布前未知项不会预填为通过。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| 发布候选分支 | `codex/skill-install-v050-rebuild` |
| 目标版本 / 架构 | `v0.6.0` / macOS Apple Silicon |
| 目标附件 | `CSSwitch_0.6.0_aarch64.dmg` |
| 最终 peeled tag commit | 发布后回填 |
| GitHub Release | 发布后回填 |

最终 source identity 以公开 `v0.6.0^{}` 为准，不能用分支名、Release 的 `targetCommitish` 或预合并构建替代。

## 自动化与代码审查

候选代码与 `origin/main@aa2cd34063f75e06f29cb7ce2fabe4208d4afcc9` 合并后，在 `435d8b9a57e668026b70aa4b20fcc4e51e84deb2` 运行：

```text
bash test/run_all.sh --require-release-ready
```

结果退出 0：offline、loopback、scripts、rust、frontend 五层全部 `pass`，汇总为 `current-env clean: YES` 与 `release-ready green: YES`。其中 Tauri 为 232 passed / 3 explicit ignored，Gateway 为 102 passed。ignored 项是需要显式启动的隔离 runtime / real-Science E2E，不计作自动通过。

此前同一功能分支还分别完成 Gateway、Tauri、`skill-package`、bridge/runtime Python 边界测试，三个 Rust crate 的 `cargo clippy -- -D warnings`，以及 GitHub bundle、同提交复用、网络失败、无效提交、本地包和 bundle 卸载确认的定向验证。发布构建仍必须从最终 tag 对应的精确提交重新生成。

## 发布者人工验收

发布者在 2026-07-16 报告“大部分验收成功”。这提供了继续合并发布的人工授权，但没有逐项结构化日志，因此只记作部分真机验收，不扩写成完整矩阵全部通过。本轮验收清单覆盖真实 Science / 千问交互、固定 Nature URL 安装与同提交复用、bundle 取消确认、停止后恢复以及本地包导入路线；发布者没有逐项说明其中哪些属于尚未完成的少数项。最终对外结论仍以发布者实际操作和公开 artifact 身份分别判断。

## 最终附件身份

以下项目必须在最终 commit 构建和 GitHub 上传后回填：

| 项目 | 值 |
|---|---|
| 本地最终 DMG 路径 | `desktop/src-tauri/target/release/bundle/dmg/CSSwitch_0.6.0_aarch64.dmg` |
| 本地大小 / SHA-256 | 发布后回填 |
| GitHub asset 大小 / digest | 发布后回填 |
| 本地与远端字节身份 | 发布后回填 |

预合并 DMG 不作为发布附件，也不以其 hash 证明最终 release。

## 签名、notarization 与 Gatekeeper

v0.6.0 仍按现有 ad-hoc 分发链路构建；最终 app 的 `codesign --verify --deep --strict`、签名身份、DMG 校验、notarization、stapled ticket 和 Gatekeeper 评估必须逐项回填。ad-hoc seal 验证通过不等于 Developer ID 签名或 Apple 公证。

## 尚未建立或不在本次声明中的层

- Developer ID 签名、Apple notarization、stapled ticket 与 Gatekeeper acceptance；
- 所有 provider、所有 Skill 领域功能和真实 SSH server 的穷举验证；
- 未由发布者逐项记录的完整真机验收矩阵；
- 私有仓库、覆盖更新、部分 bundle 物理删除、Skill Manager / catalog / inventory / SQLite 集成。
